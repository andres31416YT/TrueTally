---
# =============================================================================
# ROLE: api_gateway
# =============================================================================
# Responsabilidades:
# - Crear/obtener API Gateway HTTP API (punto de entrada público)
# - Crear integraciones Lambda para acceso y despachador
# - Configurar permisos para que API Gateway pueda invocar las Lambdas
# - Crear rutas: POST /vote (despachador) y catch-all (acceso)
# - Configurar stage con auto-deploy
# =============================================================================

- name: Crear o obtener API Gateway HTTP API
  # Crea la API si no existe, o devuelve el ID de la existente
  # La API es HTTP (no REST) y se llama <prefix>-api
  shell: |
    API_ID=$(aws apigatewayv2 get-apis --query "Items[?Name=='{{ name_prefix }}-api'].ApiId | [0]" --output text --region "{{ aws_region }}")
    if [ "$API_ID" = "None" ] || [ -z "$API_ID" ]; then
      aws apigatewayv2 create-api \
        --name "{{ name_prefix }}-api" \
        --protocol-type HTTP \
        --region "{{ aws_region }}" \
        --query 'ApiId' \
        --output text
    else
      echo "$API_ID"
    fi
  register: api_id_result

- name: Obtener API ID
  set_fact:
    api_id: "{{ api_id_result.stdout }}"

- name: Limpiar APIs duplicadas (mantener solo el API principal)
  # Elimina APIs huérfanas con el mismo nombre (pueden quedar de despliegues previos)
  # Solo mantiene la API principal devuelta por la tarea anterior
  shell: |
    API_IDS=$(aws apigatewayv2 get-apis --query "Items[?Name=='{{ name_prefix }}-api'].ApiId" --output text --region "{{ aws_region }}")
    PRIMARY_API="{{ api_id }}"
    for API_ID in $API_IDS; do
      if [ "$API_ID" != "$PRIMARY_API" ] && [ "$API_ID" != "None" ]; then
        echo "Eliminando API duplicada: $API_ID"
        aws apigatewayv2 delete-api --api-id "$API_ID" --region "{{ aws_region }}" 2>/dev/null || true
      fi
    done
  ignore_errors: true

- name: Crear o obtener integración Lambda para acceso
  # Crea la integración AWS_PROXY entre API Gateway y lambda-acceso
  # Si ya existe, devuelve el IntegrationId existente (idempotente)
  shell: |
    INTEGRATION_ID=$(aws apigatewayv2 get-integrations --api-id "{{ api_id }}" --query "Items[?IntegrationUri=='arn:aws:apigateway:{{ aws_region }}:lambda:path/2015-03-31/functions/arn:aws:lambda:{{ aws_region }}:{{ aws_account_id }}:function:{{ name_prefix }}-acceso/invocations'].IntegrationId | [0]" --output text --region "{{ aws_region }}")
    if [ "$INTEGRATION_ID" = "None" ] || [ -z "$INTEGRATION_ID" ]; then
      aws apigatewayv2 create-integration \
        --api-id "{{ api_id }}" \
        --integration-type AWS_PROXY \
        --integration-uri "arn:aws:apigateway:{{ aws_region }}:lambda:path/2015-03-31/functions/arn:aws:lambda:{{ aws_region }}:{{ aws_account_id }}:function:{{ name_prefix }}-acceso/invocations" \
        --payload-format-version "2.0" \
        --region "{{ aws_region }}" \
        --query 'IntegrationId' \
        --output text
    else
      echo "$INTEGRATION_ID"
    fi
  register: integration_acceso

- name: Crear o obtener integración Lambda para despachador
  # Crea la integración AWS_PROXY entre API Gateway y lambda-despachador
  # Esta lambda recibe los votos desde el frontend vía POST /vote
  shell: |
    INTEGRATION_ID=$(aws apigatewayv2 get-integrations --api-id "{{ api_id }}" --query "Items[?IntegrationUri=='arn:aws:apigateway:{{ aws_region }}:lambda:path/2015-03-31/functions/arn:aws:lambda:{{ aws_region }}:{{ aws_account_id }}:function:{{ name_prefix }}-despachador/invocations'].IntegrationId | [0]" --output text --region "{{ aws_region }}")
    if [ "$INTEGRATION_ID" = "None" ] || [ -z "$INTEGRATION_ID" ]; then
      aws apigatewayv2 create-integration \
        --api-id "{{ api_id }}" \
        --integration-type AWS_PROXY \
        --integration-uri "arn:aws:apigateway:{{ aws_region }}:lambda:path/2015-03-31/functions/arn:aws:lambda:{{ aws_region }}:{{ aws_account_id }}:function:{{ name_prefix }}-despachador/invocations" \
        --payload-format-version "1.0" \
        --region "{{ aws_region }}" \
        --query 'IntegrationId' \
        --output text
    else
      aws apigatewayv2 update-integration \
        --api-id "{{ api_id }}" \
        --integration-id "$INTEGRATION_ID" \
        --payload-format-version "1.0" \
        --region "{{ aws_region }}" >/dev/null
      echo "$INTEGRATION_ID"
    fi
  register: integration_despachador

- name: Garantizar permiso a API Gateway para invocar lambda-acceso
  # Agrega el permiso lambda:InvokeFunction a la policy de lambda-acceso
  # Solo lo agrega si no existe (idempotente, no falla en re-ejecuciones)
  # El statement ID único 'api-gateway-access-acceso' evita duplicados
  shell: |
    STATEMENT_ID="api-gateway-access-acceso"
    FUNCTION_NAME="{{ name_prefix }}-acceso"
    REGION="{{ aws_region }}"
    API_ID="{{ api_id }}"
    ACCOUNT_ID="{{ aws_account_id }}"
    SOURCE_ARN="arn:aws:execute-api:${REGION}:${ACCOUNT_ID}:${API_ID}/*/*/*"

    EXISTING=$(aws lambda get-policy --function-name "$FUNCTION_NAME" --region "$REGION" --query "contains(Policy, \`$STATEMENT_ID\`)" --output text 2>/dev/null || echo "None")

    if [ "$EXISTING" != "True" ]; then
      aws lambda add-permission \
        --function-name "$FUNCTION_NAME" \
        --statement-id "$STATEMENT_ID" \
        --action "lambda:InvokeFunction" \
        --principal "apigateway.amazonaws.com" \
        --source-arn "$SOURCE_ARN" \
        --region "$REGION"
    fi

- name: Garantizar permiso a API Gateway para invocar lambda-despachador
  # Agrega el permiso lambda:InvokeFunction a la policy de lambda-despachador
  # Solo lo agrega si no existe (idempotente)
  shell: |
    STATEMENT_ID="api-gateway-access-despachador"
    FUNCTION_NAME="{{ name_prefix }}-despachador"
    REGION="{{ aws_region }}"
    API_ID="{{ api_id }}"
    ACCOUNT_ID="{{ aws_account_id }}"
    SOURCE_ARN="arn:aws:execute-api:${REGION}:${ACCOUNT_ID}:${API_ID}/*/*/*"

    EXISTING=$(aws lambda get-policy --function-name "$FUNCTION_NAME" --region "$REGION" --query "contains(Policy, \`$STATEMENT_ID\`)" --output text 2>/dev/null || echo "None")

    if [ "$EXISTING" != "True" ]; then
      aws lambda add-permission \
        --function-name "$FUNCTION_NAME" \
        --statement-id "$STATEMENT_ID" \
        --action "lambda:InvokeFunction" \
        --principal "apigateway.amazonaws.com" \
        --source-arn "$SOURCE_ARN" \
        --region "$REGION"
    fi

- name: Crear o actualizar ruta POST /vote
  # Ruta principal que recibe votos desde el frontend
  # Apunta a la integración de lambda-despachador
  shell: |
    ROUTE_ID=$(aws apigatewayv2 get-routes --api-id "{{ api_id }}" --query "Items[?RouteKey=='POST /vote'].RouteId | [0]" --output text --region "{{ aws_region }}")
    if [ "$ROUTE_ID" = "None" ] || [ -z "$ROUTE_ID" ]; then
      aws apigatewayv2 create-route \
        --api-id "{{ api_id }}" \
        --route-key "POST /vote" \
        --target "integrations/{{ integration_despachador.stdout }}" \
        --region "{{ aws_region }}"
    else
      aws apigatewayv2 update-route \
        --api-id "{{ api_id }}" \
        --route-id "$ROUTE_ID" \
        --target "integrations/{{ integration_despachador.stdout }}" \
        --region "{{ aws_region }}"
    fi
  ignore_errors: true

- name: Crear o actualizar ruta catch-all POST
  # Ruta comodín para cualquier otro POST (registro, login, etc.)
  # Apunta a la integración de lambda-acceso
  shell: |
    ROUTE_ID=$(aws apigatewayv2 get-routes --api-id "{{ api_id }}" --query "Items[?RouteKey=='POST /{proxy+}'].RouteId | [0]" --output text --region "{{ aws_region }}")
    if [ "$ROUTE_ID" = "None" ] || [ -z "$ROUTE_ID" ]; then
      aws apigatewayv2 create-route \
        --api-id "{{ api_id }}" \
        --route-key "POST /{proxy+}" \
        --target "integrations/{{ integration_acceso.stdout }}" \
        --region "{{ aws_region }}"
    else
      aws apigatewayv2 update-route \
        --api-id "{{ api_id }}" \
        --route-id "$ROUTE_ID" \
        --target "integrations/{{ integration_acceso.stdout }}" \
        --region "{{ aws_region }}"
    fi
  ignore_errors: true

- name: Crear o actualizar ruta catch-all GET
  # Ruta comodín para cualquier GET (bloques, health check, etc.)
  # Apunta a la integración de lambda-acceso
  shell: |
    ROUTE_ID=$(aws apigatewayv2 get-routes --api-id "{{ api_id }}" --query "Items[?RouteKey=='GET /{proxy+}'].RouteId | [0]" --output text --region "{{ aws_region }}")
    if [ "$ROUTE_ID" = "None" ] || [ -z "$ROUTE_ID" ]; then
      aws apigatewayv2 create-route \
        --api-id "{{ api_id }}" \
        --route-key "GET /{proxy+}" \
        --target "integrations/{{ integration_acceso.stdout }}" \
        --region "{{ aws_region }}"
    else
      aws apigatewayv2 update-route \
        --api-id "{{ api_id }}" \
        --route-id "$ROUTE_ID" \
        --target "integrations/{{ integration_acceso.stdout }}" \
        --region "{{ aws_region }}"
    fi
  ignore_errors: true

- name: Crear o actualizar stage con auto-deploy
  # Configura el stage $default con auto-deploy activado
  # Cualquier cambio en rutas/integraciones se despliega automáticamente
  shell: |
    aws apigatewayv2 create-stage \
      --api-id "{{ api_id }}" \
      --stage-name "\$default" \
      --auto-deploy \
      --region "{{ aws_region }}" 2>/dev/null || \
    aws apigatewayv2 update-stage \
      --api-id "{{ api_id }}" \
      --stage-name "\$default" \
      --auto-deploy \
      --region "{{ aws_region }}"
  ignore_errors: true

- name: Configurar CORS en API Gateway
  # Habilita CORS para que el frontend pueda consumir la API desde el navegador
  # Idempotente: si ya tiene configuración, no se modifica
  shell: |
    CURRENT=$(aws apigatewayv2 get-api --api-id "{{ api_id }}" --query 'Api.CorsConfiguration.AllowOrigins | [0]' --output text --region "{{ aws_region }}" 2>/dev/null || echo "None")
    if [ "$CURRENT" = "None" ] || [ -z "$CURRENT" ]; then
      aws apigatewayv2 update-api \
        --api-id "{{ api_id }}" \
        --cors-configuration '{
          "AllowOrigins": ["*"],
          "AllowMethods": ["GET","POST","OPTIONS"],
          "AllowHeaders": ["Content-Type","Authorization","X-User-Email","X-User-Role","X-Amz-Date","X-Api-Key","X-Amz-Security-Token"],
          "MaxAge": 86400
        }' \
        --region "{{ aws_region }}"
    fi

- name: Obtener URL del API Gateway
  # Construye la URL pública de la API para que el frontend la use
  set_fact:
    api_gateway_url: "https://{{ api_id }}.execute-api.{{ aws_region }}.amazonaws.com"

- name: Mostrar URL del API Gateway
  debug:
    msg: "API Gateway URL: {{ api_gateway_url }}"
